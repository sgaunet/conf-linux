---

# "task" version to install
task_version: "v3.19.0"
task_bin_path: "/usr/local/bin"
task_tmp_directory: "{{ lookup('env', 'TMPDIR') | default('/tmp',true) }}"
task_os: "linux" 
task_arch: "amd64" 