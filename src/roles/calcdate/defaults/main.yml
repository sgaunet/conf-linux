---

# "calcdate" version to install
calcdate_version: "1.4.3"
calcdate_bin_path: "/usr/local/bin"
calcdate_tmp_directory: "{{ lookup('env', 'TMPDIR') | default('/tmp', true) }}"
calcdate_os: "Linux"
calcdate_arch: "x86_64"
