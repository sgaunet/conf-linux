---

# "rclone" version to install
rclone_version: "1.62.2"
rclone_bin_path: "/usr/bin"
rclone_tmp_directory: "{{ lookup('env', 'TMPDIR') | default('/tmp', true) }}"
rclone_os: "linux"
rclone_arch: "amd64"

# https://downloads.rclone.org/v1.62.2/rclone-v1.62.2-linux-amd64.zip