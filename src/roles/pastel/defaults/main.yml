---

# "pastel" version to install
pastel_version: "0.9.0"
pastel_bin_path: "/usr/local/bin"
pastel_tmp_directory: "{{ lookup('env', 'TMPDIR') | default('/tmp', true) }}"
pastel_os: "unknown-linux-gnu"
pastel_arch: "x86_64"
