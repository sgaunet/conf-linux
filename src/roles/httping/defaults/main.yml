---

httping_version: "0.3.1"
httping_bin_path: "/usr/local/bin"
httping_tmp_directory: "{{ lookup('env', 'TMPDIR') | default('/tmp',true) }}"
httping_os: "Linux" # use "darwin" for MacOS X, "windows" for Windows
httping_arch: "x86_64" # other possible values: "386","arm64","arm","ppc64le","s390x"