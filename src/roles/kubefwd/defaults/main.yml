---

# "kubefwd" version to install
kubefwd_version: "1.22.5"
kubefwd_bin_path: "/usr/local/bin"
kubefwd_tmp_directory: "{{ lookup('env', 'TMPDIR') | default('/tmp', true) }}"
kubefwd_os: "Linux"
kubefwd_arch: "x86_64"
