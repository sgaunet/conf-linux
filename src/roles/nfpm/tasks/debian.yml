---

- name: install repo
  copy:
    src: "goreleaser.list"
    dest: "/etc/apt/sources.list.d/goreleaser.list"
    mode: '0644'

- name: Install nfpm
  apt:
    name: nfpm
    state: present
    update_cache: yes