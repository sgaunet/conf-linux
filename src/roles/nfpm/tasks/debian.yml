---

- name: Install repo
  ansible.builtin.copy:
    src: "goreleaser.list"
    dest: "/etc/apt/sources.list.d/goreleaser.list"
    mode: '0644'

- name: Install nfpm
  ansible.builtin.apt:
    name: nfpm
    state: present
    update_cache: true
