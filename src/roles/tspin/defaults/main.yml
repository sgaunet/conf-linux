---

# "tspin" version to install
tspin_version: "2.2.0"
tspin_bin_path: "/usr/local/bin"
tspin_tmp_directory: "{{ lookup('env', 'TMPDIR') | default('/tmp', true) }}"
tspin_os: "unknown-linux-musl"
tspin_arch: "x86_64"
