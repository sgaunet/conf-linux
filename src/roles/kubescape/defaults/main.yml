---

# "kubescape" version to install
kubescape_version: "3.0.8"
kubescape_bin_path: "/usr/local/bin"
kubescape_tmp_directory: "{{ lookup('env', 'TMPDIR') | default('/tmp', true) }}"
kubescape_os: "ubuntu"
