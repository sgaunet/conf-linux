---

eksctl_version: "0.77.0"
eksctl_bin_path: "/usr/local/bin"
eksctl_tmp_directory: "{{ lookup('env', 'TMPDIR') | default('/tmp',true) }}"
eksctl_os: "Linux" # use "darwin" for MacOS X, "windows" for Windows
eksctl_arch: "amd64" # other possible values: "386","arm64","arm","ppc64le","s390x"