---

# "kubectlice" version to install
kubectlice_version: "0.2.10"
kubectlice_bin_path: "/usr/local/bin"
kubectlice_tmp_directory: "{{ lookup('env', 'TMPDIR') | default('/tmp',true) }}"
kubectlice_os: "linux" 
kubectlice_arch: "amd64" 