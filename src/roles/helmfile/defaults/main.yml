---

# "helmfile" version to install
helmfile_version: "0.163.0"
helmfile_bin_path: "/usr/local/bin"
helmfile_tmp_directory: "{{ lookup('env', 'TMPDIR') | default('/tmp', true) }}"
helmfile_os: "linux"
helmfile_arch: "amd64"
