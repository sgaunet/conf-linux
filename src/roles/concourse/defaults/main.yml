---

# "concourse" version to install
concourse_version: "7.9.1"
concourse_bin_path: "/usr/local/bin"
concourse_tmp_directory: "{{ lookup('env', 'TMPDIR') | default('/tmp', true) }}"
concourse_os: "linux"
concourse_arch: "amd64"
